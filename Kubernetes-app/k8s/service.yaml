apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort   # Or LoadBalancer if using cloud
  selector:
    app: flask-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080   # optional, NodePort range: 30000-32767

